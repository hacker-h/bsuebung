stages:
  - init
  - build

info:
  stage: init
  image: docker:dind
  script:
    - pwd
    - env
    - cat /etc/*release
    - ls -la
    - whoami
    - which docker

build:
  stage: build
  image: docker:dind
  script:
    - docker build -t ${CI_REGISTRY_IMAGE}:build-${CI_BUILD_ID} .
